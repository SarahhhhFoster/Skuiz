<?xml version="1.0" encoding="UTF-8"?>
<!--
  Info.plist for a Skuiz AUv3 app extension target.

  Replace: ${VENDOR_CODE} (exactly 4 characters, e.g. Skuz), ${SUBTYPE_CODE}
  (exactly 4 characters, unique per plugin), the bundle identifier, and the
  name. `type` is aufx for an effect; use aumu for an instrument.

  The AUv3 bundle is built by Xcode and embedded in a containing app; the
  Rust staticlib built from your plugin crate is linked into the extension
  target, and the principal class calls the skuiz_auv3_* C ABI.
-->
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>en</string>
    <key>CFBundleDisplayName</key>
    <string>Skuiz Plugin</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>XPC!</string>
    <key>CFBundleShortVersionString</key>
    <string>0.1.0</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>NSExtension</key>
    <dict>
        <key>NSExtensionAttributes</key>
        <dict>
            <key>AudioComponents</key>
            <array>
                <dict>
                    <key>description</key>
                    <string>Skuiz Plugin</string>
                    <key>manufacturer</key>
                    <string>${VENDOR_CODE}</string>
                    <key>name</key>
                    <string>Skuiz: Plugin</string>
                    <key>subtype</key>
                    <string>${SUBTYPE_CODE}</string>
                    <key>type</key>
                    <string>aufx</string>
                    <key>version</key>
                    <integer>1</integer>
                    <!-- Required for an out-of-process (v3) audio unit. -->
                    <key>sandboxSafe</key>
                    <true/>
                    <key>tags</key>
                    <array>
                        <string>Effects</string>
                    </array>
                </dict>
            </array>
        </dict>
        <key>NSExtensionPointIdentifier</key>
        <string>com.apple.AudioUnit-UI</string>
        <key>NSExtensionPrincipalClass</key>
        <string>$(PRODUCT_MODULE_NAME).SkuizAudioUnitViewController</string>
    </dict>
</dict>
</plist>
